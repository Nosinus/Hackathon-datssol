# docs/input/archive/binaries

Legacy binary archive for source backups (`.pdf`, `.docx`).

This folder is **not** canonical for:
- markdown summaries,
- active OpenAPI snapshots,
- release-hour DatsSol imports.

Use:
- `docs/input/` for canonical text/contract sources,
- `docs/input/datssol_imports/` for incoming official DatsSol release materials.
